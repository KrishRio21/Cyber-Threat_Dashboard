import { Container } from 'react-bootstrap';

    function Settings() {
      return (
        <Container className="mt-4 text-center">
          <h1>Settings</h1>
          <p>Placeholder for future settings (e.g., theme toggle, API key management).</p>
        </Container>
      );
    }

    export default Settings;